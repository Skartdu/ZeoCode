# 生产环境（部署时按需修改）

# 应用预览基础域名
# 生产环境：若前端和后端同域部署，可使用相对路径 /api/static
# 若跨域部署，填写完整 URL（如 https://your-domain.com/api/static），并确保 CORS 允许
VITE_APP_PREVIEW_BASE_URL=/api/static

# 部署后应用的访问域名（留空则使用后端返回的完整 URL）
VITE_APP_DEPLOY_BASE_URL=
